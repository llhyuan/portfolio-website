.slash {
  transform: scale(40);
}
.slash:hover {
  animation-name: slash-landing;
  animation-timing-function: ease-in;
  animation-duration: 1s;
  animation-iteration-count: 4;
}

@keyframes slash-landing {
  0% {
      transform: scale(40);
  }
  50%{
      transform: scale(20);
  }
  100% {
      transform: scale(0);
  }
}
